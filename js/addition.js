function addTen(String) {
    console.log(String);
    var data = {"one":1, "two": "two"}
    var sum = 0;
    // for (let i = 0; i < 1000000; i++) {
    //    sum = sum +i;        
    // }
    return data;
}